Be aware of the raw pointer that points to data in free-store
 bool *check = new bool[N]; // make base of seive of Erotosthenes
 It will kill your program due to the overflow because you do not delete the data in free-store after using it.
